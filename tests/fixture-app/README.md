# fixture-app

A tiny CLI task manager used as an AgentWiki test fixture.

- `src/main.py` — CLI entry point (argparse)
- `src/api.py` — task operations
- `src/models.py` — `Task` dataclass
- `src/storage.py` — SQLite persistence
- `db/schema.sql` — schema
