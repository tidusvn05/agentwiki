## Database Overview

### Summary

| Metric | Count |
|--------|-------|
| Database Projects | 0 |
| Tables | 1 |
| Views | 0 |
| Stored Procedures | 0 |
| Functions | 0 |
| Relationships | 0 |

### Tables

#### `main.tasks`

Bảng lưu trữ các tác vụ (task) của ứng dụng fixture quản lý công việc. Mỗi hàng gồm khóa chính tự tăng id, tiêu đề bắt buộc title và cờ trạng thái hoàn thành done (0/1). Có chỉ mục idx_tasks_done trên cột done để tối ưu truy vấn lọc theo trạng thái. Schema được tạo lũy đẳng bằng IF NOT EXISTS.

| Column | Type | Nullable | Identity | Default |
|--------|------|----------|----------|---------|
| id | INTEGER | false | true |  |
| title | TEXT | false | false |  |
| done | INTEGER | false | false | 0 |

**Primary key**: id

**Source**: `tests/fixture-app/db/schema.sql`

### Data Flows

#### Thêm tác vụ mới (CLI add)

- tests/fixture-app/src/main.py (CLI) → TaskAPI.add (src/api.py) → Storage.insert (src/storage.py) → Bảng tasks (SQLite, file tasks.db)
- Operations: INSERT

#### Liệt kê tác vụ (CLI list)

- Bảng tasks → TaskAPI.list → main.py in ra stdout
- Operations: SELECT

#### Đánh dấu hoàn thành (CLI done)

- tests/fixture-app/src/main.py (CLI) → TaskAPI.done (src/api.py) → Storage.mark_done (src/storage.py) → Bảng tasks (cập nhật cột done theo title)
- Operations: UPDATE


---

**Analysis Confidence**: 3.5/10
