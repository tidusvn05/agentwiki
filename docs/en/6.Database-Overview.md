## Database Overview

### Summary

| Metric | Count |
|--------|-------|
| Database Projects | 0 |
| Tables | 1 |
| Views | 0 |
| Stored Procedures | 0 |
| Functions | 0 |
| Relationships | 0 |

### Tables

#### `main.tasks`

SQLite table storing task items for the test fixture application. `id` is an AUTOINCREMENT primary key, `title` holds the task name, and `done` is an integer flag (0/1) tracking completion. A supporting index `idx_tasks_done` on `done` speeds up filtering by completion status.

| Column | Type | Nullable | Identity | Default |
|--------|------|----------|----------|---------|
| id | INTEGER | false | true |  |
| title | TEXT | false | false |  |
| done | INTEGER | false | false | 0 |

**Primary key**: id

**Source**: `tests/fixture-app/db/schema.sql`

### Data Flows

#### Task persistence via Storage DAO

- tests/fixture-app/src/storage.py (Storage class methods insert/all/mark_done) → tasks
- Operations: INSERT INTO tasks (title, done) — insert(), SELECT id, title, done FROM tasks ORDER BY id — all(), UPDATE tasks SET done = 1 WHERE title = ? — mark_done()


---

**Analysis Confidence**: 3.0/10
